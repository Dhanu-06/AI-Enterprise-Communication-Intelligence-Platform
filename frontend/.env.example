# Local development — browser calls Next.js rewrite proxy
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# Vercel / Docker server-side fetch (absolute URL to backend API prefix)
# INTERNAL_API_URL=https://your-backend.up.railway.app/api/v1
# BACKEND_INTERNAL_URL=https://your-backend.up.railway.app
